from enum import Enum #Enum es una clase que permite crear enumeraciones

class MMM(Enum): #MMM es una clase que hereda de Enum
    MEDIA = 1 #MEDIA es un atributo de la clase MMM
    MODA = 2 #MODA es un atributo de la clase MMM